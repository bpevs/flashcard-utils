{
  "fmt": {
    "proseWrap": "never",
    "singleQuote": true,
    "semiColons": false
  },
  "tasks": {
    "test": "deno fmt && deno lint && deno test -A",
    "test:update": "deno test -A -- --update",
    "dev": "deno run --allow-read --allow-write --allow-net --watch sandbox/main.tsx"
  }
}
